---
title: "Authorization"
---

All Originpay API requests require secure authentication using access tokens generated through the Authorization API.

To securely interact with Originpay services, your backend must first generate an authorization token using the Auth token endpoint. The generated token is required for all subsequent API requests and remains valid until the `expires_at` timestamp.

## Generate Authorization Token

### Sandbox Environment

`POST https://api-sand.originpay.in/v2/auth/token`

### Production Environment

`POST https://api.originpay.in/v2/auth/token`

### Request Headers

| Header | Value |
| :-- | :-- |
| Content-Type | application/x-www-form-urlencoded |

### Request Parameters

| Parameter | Description |
| :-- | :-- |
| public\_key | Public key shared by Originpay |
| secret\_key | Secret key shared by Originpay |

### Sample Request

```json
{
  "public_key": "<your_public_key>",
  "secret_key": "<your_secret_key>",
}
```

### Sample Response

```json
{
    "success": true,
    "data": {
        "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXV........",
        "expires_in": 3600,
        "issued_at": 1778689560,
        "expires_at": 1778693160,
        "token_type": "Bearer"
    }
}
```

### Important Notes

- The `access_token` must be included in all authenticated API requests
- Tokens should be refreshed before the `expires_at` timestamp
- Never expose API credentials in frontend or client-side applications
- Store credentials securely using environment variables or secret managers